---
title: Daily briefing
layout: base.njk
pagination:
    data: news
    size: 1
    alias: news
permalink: /{{ news.country }}/index.html
---

<div class="container">
    <h1>Here is your page</h1>
    <div class="vibe">You are great!</div>
    <div class="love">❤</div>
    <ul>
        {% for article in news.articles %}
        <li>
            <h3><a href="{{ article.url }}">{{ article.title }}</a></h3>
            <span>
                <p>{{ article.source.name }} - <time>{{ article.publishedAt }}</time></p>
            </span>
        </li>
        {% endfor %}
    </ul>
</div>
<script src="/js/vibe.js"></script>